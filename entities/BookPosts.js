class BookPosts {
    constructor(type, commentsNum, userId, id) {
        this.type = type
        this.commentsNum = commentsNum
        this.userId = userId
        this.id = id
    }
}

export { BookPosts }